name: audiobook_converter
description: App to convert and listen books from different format

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  permission_handler: ^9.2.0
  file_manager: ^1.0.0
  file_picker_cross: ^4.6.0
  pdf_render: ^1.1.0
  image: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
